div.transition
  strong Notice:
  = ' because of no.de being discontinued, the log server has moved to '
  a(href='http://logs.libuv.org/#{key}/index') logs.libuv.org/#{key}/index
  = '.'

div.navigation
  span.title #{channel}
  = ' | '
  span.nolink index
  = ' | '
  a(href='latest') latest

div.index
  ul
    - each date in dates
      li
        a(href= date) #{date}
        - if (date == dates[0])
          !{' (<a href="latest">latest</a>)'}